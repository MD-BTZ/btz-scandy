version: '3.8'

services:
  scandy_technik-mongodb:
    image: mongo:7.0
    container_name: scandy_technik-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin123
      MONGO_INITDB_DATABASE: scandy
    ports:
      - "27017:27017"
    volumes:
      - ./scandy_testilol/mongodb:/data/db
      - ./mongo-init:/docker-entrypoint-initdb.d
    networks:
      - scandy_technik-network
    command: mongod --auth --bind_ip_all
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  scandy_technik-mongo-express:
    image: mongo-express:1.0.0
    container_name: scandy_technik-mongo-express
    restart: unless-stopped
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: admin123
      ME_CONFIG_MONGODB_URL: mongodb://admin:admin123@scandy_technik-mongodb:27017/
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: admin123
    ports:
      - "8085:8081"
    depends_on:
      - scandy_technik-mongodb
    networks:
      - scandy_technik-network

  scandy_technik-app:
    build: .
    container_name: scandy_technik-app
    restart: unless-stopped
    environment:
      - DATABASE_MODE=mongodb
      - MONGODB_URI=mongodb://admin:admin123@scandy_technik-mongodb:27017/
      - MONGODB_DB=scandy
      - FLASK_ENV=production
      - SECRET_KEY=scandy-secret-key-klB3cOmh
      - SYSTEM_NAME=Scandy
      - TICKET_SYSTEM_NAME=Aufgaben
      - TOOL_SYSTEM_NAME=Werkzeuge
      - CONSUMABLE_SYSTEM_NAME=Verbrauchsgüter
      - CONTAINER_NAME=scandy_technik
      - TZ=Europe/Berlin
    ports:
      - "5005:5000"
    volumes:
      - ./scandy_testilol/uploads:/app/app/uploads
      - ./scandy_testilol/backups:/app/app/backups
      - ./scandy_testilol/logs:/app/app/logs
      - ./scandy_testilol/static:/app/app/static
    depends_on:
      - scandy_technik-mongodb
    networks:
      - scandy_technik-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  scandy_technik-mongodb-data:
    driver: local

networks:
  scandy_technik-network:
    driver: bridge 